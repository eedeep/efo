


def verbose_app_label(request):
    """
    Change the app_label to VERBOSE_APP_LABEL as optionally found in
    <app_name>/settings.py
    
    e.g.
    
        VERBOSE_APP_LABEL = 'verbose app name'
    """
    # import ipdb; ipdb.set_trace()
    
    # iterate through the app_list looking for a corresponding app with
    # a VERBOSE_APP_LABEL
    
    return {}